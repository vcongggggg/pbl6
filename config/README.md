# Configuration Templates

Thư mục `config/` chứa các file mẫu cấu hình không nhạy cảm (non-secret templates) và tài liệu hướng dẫn thiết lập hệ thống cho từng môi trường (Development, Testing, Lab Simulation).
