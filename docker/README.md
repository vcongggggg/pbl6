# Docker Configurations & Compose Files

Thư mục `docker/` chứa các cấu hình bổ sung và file định nghĩa môi trường container hóa chi tiết cho các dịch vụ.
File điều phối chính của toàn bộ dự án đặt tại gốc repository: `docker-compose.yml`.
